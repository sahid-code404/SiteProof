# Infrastructure

Phase 1 uses root-level `docker-compose.yml` for local development. Future deployment-specific reverse proxy, TLS, object-storage provisioning, observability, and environment manifests should live here rather than inside application code.
